{
  "name": "blogging-website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "parcel build src/*.html --dist-dir dist --public-url ./"
  },
  "devDependencies": {
    "parcel": "^2.10.0"
  }
}